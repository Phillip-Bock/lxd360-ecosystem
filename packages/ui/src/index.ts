/**
 * @lxd360/ui
 * Shared UI components for web and mobile
 *
 * Components are designed to be platform-agnostic where possible,
 * with platform-specific implementations when needed.
 */

// Re-export components
export * from './components/button';

// Utility exports
export { cn } from './utils';
